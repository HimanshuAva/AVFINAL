/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: ["class"],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
  	container: {
  		center: true,
  		padding: '2rem',
  		screens: {
  			'2xl': '1400px'
  		}
  	},
  	extend: {
  		colors: {
  			primary: '#001f3f', // Deep Navy
  			accent: '#F7C948', // Warm Yellow/Gold
  			background: '#f1efec', // Soft Beige
  			'off-white': '#ffffff',
  			brand: {
  				navy: '#0C2340',
  				gold: '#F7C948',
  				mutedGrey: '#F0F0F0',
  			},
  			'gradient-from': '#ffd319',
  			'gradient-via': '#ff2975',
  			'gradient-to': '#8c1eff',
  			// Keep some of the existing color variables for compatibility
  			border: 'hsl(var(--border))',
  			input: 'hsl(var(--input))',
  			ring: 'hsl(var(--ring))',
  			foreground: '#0C2340',
  			secondary: {
  				DEFAULT: '#F0F0F0',
  				foreground: '#0C2340'
  			},
  			destructive: {
  				DEFAULT: 'hsl(var(--destructive))',
  				foreground: 'hsl(var(--destructive-foreground))'
  			},
  			muted: {
  				DEFAULT: '#F0F0F0',
  				foreground: '#64748b'
  			},
  			popover: {
  				DEFAULT: '#ffffff',
  				foreground: '#0C2340'
  			},
  			card: {
  				DEFAULT: '#ffffff',
  				foreground: '#0C2340'
  			},
  			chart: {
  				'1': 'hsl(var(--chart-1))',
  				'2': 'hsl(var(--chart-2))',
  				'3': 'hsl(var(--chart-3))',
  				'4': 'hsl(var(--chart-4))',
  				'5': 'hsl(var(--chart-5))'
  			}
  		},
  		borderRadius: {
  			lg: 'var(--radius)',
  			md: 'calc(var(--radius) - 2px)',
  			sm: 'calc(var(--radius) - 4px)'
  		},
  		keyframes: {
  			'accordion-down': {
  				from: {
  					height: 0
  				},
  				to: {
  					height: 'var(--radix-accordion-content-height)'
  				}
  			},
  			'accordion-up': {
  				from: {
  					height: 'var(--radix-accordion-content-height)'
  				},
  				to: {
  					height: 0
  				}
  			}
  		},
  		animation: {
  			'accordion-down': 'accordion-down 0.2s ease-out',
  			'accordion-up': 'accordion-up 0.2s ease-out'
  		}
  	}
  },
  plugins: [require("tailwindcss-animate")],
} 